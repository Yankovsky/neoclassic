.event
  .article-icon
  .content-block
    .content
      .date {{event.datetime | date:"dd MMMM y"}}
      h1 {{event.title}}
      .text {{event.text}}
      a(ng-href='/events/{{event.urlSlug}}') подробнее
  img(ng-src='{{event.imgUrl}}')